# VDashboard - 主播展示平台

一个现代化的主播展示平台，支持 Dark Mode，展示主播信息、歌单和追番列表。

## 🚀 功能特性

- ✅ **响应式设计** - 完美支持移动、平板和桌面设备
- ✅ **Dark Mode** - 自动根据系统偏好设置，支持手动切换
- ✅ **轮播展示** - 主页轮播展示所有主播，点击可跳转到详情页
- ✅ **多歌单管理** - 每个主播支持多个歌单，第一个为特殊的点唱歌单
- ✅ **歌词显示** - 弹出模态框展示歌词，支持复制
- ✅ **智能复制** - 点唱歌单中复制歌曲名自动添加"点歌"前缀
- ✅ **动态路由** - 每个主播独立页面，`/streamer/[id]` 格式
- ✅ **SEO 优化** - 自动生成 Meta 标签

## 📁 项目结构

```
app/
├── page.tsx                      # 主页
├── layout.tsx                    # 根布局 + Dark Mode Provider
├── globals.css                   # 全局样式
├── components/
│   ├── Navbar.tsx               # 导航栏 + Dark Mode 切换
│   ├── Carousel.tsx             # 轮播组件
│   ├── CopyButton.tsx           # 复制按钮
│   ├── LyricsModal.tsx          # 歌词模态框
│   ├── ThemeProvider.tsx        # 主题提供器
│   └── ThemeToggle.tsx          # 主题切换按钮
├── lib/
│   ├── types.ts                 # TypeScript 类型定义
│   └── utils.ts                 # 工具函数（数据加载）
└── streamer/
    ├── [id]/
    │   ├── page.tsx             # 子页面模板
    │   └── not-found.tsx        # 404 页面
    └── components/
        └── TabPanel.tsx         # 标签页组件

public/
├── data/
│   ├── streamers.json           # 主播索引（轻量级，~2KB）
│   ├── videos.json              # 独立视频数据
│   └── streamers/               # 主播详情目录
│       ├── 1-名字.json          # 单个主播详情文件
│       ├── 2-名字.json
│       └── ...
└── images/
    └── streamers/               # 主播相关图片
```

## 📊 数据结构

VDashboard 采用**两层混合数据架构**，以支持高效扩展：

### Layer 1 - 索引文件 (`public/data/streamers.json`)

轻量级索引，用于主页快速加载（~2KB）：

```json
{
  "streamers": [
    {
      "id": 1,
      "name": "希罗Kirara",
      "bio": "简短介绍，显示在卡片上",
      "avatar": "/images/streamers/kirara.jpg",
      "fans": "5.2万",
      "playlistCount": 3,
      "songCount": 18,
      "file": "1-希罗Kirara.json"
    }
  ],
  "videos": [
    {
      "id": 1,
      "title": "视频标题",
      "cover": "/images/video/cover.jpg",
      "videoUrl": "https://www.bilibili.com/video/BV...",
      "description": "视频描述"
    }
  ]
}
```

### Layer 2 - 详情文件 (`public/data/streamers/X-名字.json`)

完整主播数据，在用户进入子页面时懒加载（~100-200 行）：

```json
{
  "id": 1,
  "name": "希罗Kirara",
  "bilibiliId": "12345678",
  "liveUrl": "https://www.bilibili.com/12345678",
  "bilibiliSpaceUrl": "https://space.bilibili.com/12345678",
  "avatar": "/images/streamers/kirara.jpg",
  "banner": "/images/streamers/banner.jpg",
  "bio": "简短介绍",
  "description": "详细介绍，支持多行",
  "cloudMusicUrl": "https://music.163.com/user/xxx",
  "redUrl": "https://www.xiaohongshu.com/user/xxx",
  "playlists": [
    {
      "name": "点唱曲库",
      "songs": [
        {
          "name": "歌曲名",
          "artist": "艺术家",
          "genre": "流派",
          "tag": "标签",
          "language": "中文",
          "album": "专辑",
          "lyrics": "完整歌词\n支持多行",
          "url": "https://music.url"
        }
      ]
    }
  ],
  "animes": [
    {
      "name": "番剧名",
      "episodes": 12,
      "status": "在看",
      "url": "https://anime.url"
    }
  ]
}
```

**两层架构优势**：
- 📄 主页只加载 2KB 索引，快速展示
- 🚀 子页面懒加载详情，按需下载完整数据
- 🔄 更新歌单无需修改索引，减少冲突
- 📈 支持无限扩展，单个文件保持可维护的大小

## 🔧 开发命令

```bash
# 安装依赖
pnpm install

# 启动开发服务器 (localhost:3000)
pnpm dev

# 构建生产版本
pnpm build

# 启动生产服务器
pnpm start

# 运行 ESLint
pnpm lint
```

## 🎨 Dark Mode 使用

主题切换由 `next-themes` 提供支持，自动保存用户偏好到 localStorage。

点击导航栏右上角的主题切换按钮即可在 Light/Dark 模式间切换。

## 📝 更新主播数据

VDashboard 使用两层数据结构，数据更新遵循以下规则：

### 修改歌单或番剧（最常见）

✏️ **仅编辑第 2 层详情文件**，无需修改索引：

```bash
# 编辑对应主播的详情文件，例如：
vim public/data/streamers/1-希罗Kirara.json

# 修改 playlists 或 animes 字段后保存
# 开发服务器自动热重载
```

### 添加新主播

📝 **需要同时更新第 1 层索引和第 2 层详情**：

1. **编辑索引文件** (`public/data/streamers.json`)：添加主播元数据
2. **创建详情文件** (`public/data/streamers/7-新主播.json`)：包含完整信息

详见 [DEVELOPMENT.md](DEVELOPMENT.md) "添加新主播" 或 [CUSTOMIZATION.md](CUSTOMIZATION.md)

### 完整更新流程（修改代码 + 数据）

1. 本地修改代码或数据
2. 提交到 Git：

```bash
git add .
git commit -m "update: 描述变更"
git push origin main
```

3. 在服务器拉取并重新部署：

```bash
cd /path/to/vdashboard
git pull origin main
pnpm install
pnpm build
pm2 restart vdashboard  # 如果使用 PM2
```

## 🖼️ 图片资源

### 占位图

目前所有占位图采用渐变色生成（紫粉色系）。

### 替换真实图片

将图片放入 `public/images/streamers/` 目录，然后在 JSON 中更新路径：

```json
{
  "avatar": "/images/streamers/streamer-name.jpg",
  "banner": "/images/streamers/streamer-banner.jpg"
}
```

支持的格式：JPG, PNG, WebP

## 📱 响应式设计

- **手机 (< 640px)** - 单列布局
- **平板 (640px - 1024px)** - 两列布局
- **桌面 (> 1024px)** - 三列布局

## 🌐 主播数据字段说明

- **id**: 主播唯一标识符（数字）
- **name**: 主播名称
- **bilibiliId**: B站UID（用于直播间和空间链接）
- **liveUrl**: B站直播间完整链接
- **bilibiliSpaceUrl**: B站个人空间链接
- **avatar**: 头像图片路径
- **banner**: 宣传图片路径
- **bio**: 简短自我介绍（显示在卡片上）
- **description**: 详细介绍（显示在详情页）
- **cloudMusicUrl**: 网易云音乐主页链接（可选）
- **redUrl**: 小红书主页链接（可选）
- **playlists**: 歌单数组
  - 第一个歌单自动作为"点唱歌单"，复制时添加"点歌"前缀
  - 其他歌单为普通歌单，复制只复制歌曲名

## 🔒 环境变量

暂无需要的环保变量。如后续需要 API 密钥，在 `.env.local` 中配置。

## 🚀 部署

### Vercel（推荐）

```bash
npm install -g vercel
vercel
```

### 自托管

```bash
pnpm build
pnpm start
```

或使用 PM2：

```bash
pm2 start "npm start" --name vdashboard
```

## 📄 许可证

MIT

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 支持

如有问题，请通过 GitHub Issues 反馈。
